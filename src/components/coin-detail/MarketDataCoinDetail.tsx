import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

export const MarketDataCoinDetail: React.FC<Props> = ({}) => {
    return (
        <View>
            <Text>Text</Text>
        </View>
    );
};

const styles = StyleSheet.create({});
